---
- name: User configuration for dan
  hosts: all

  vars:
    ansible_user: dan
    zsh_ssh_auth_sock: false

  roles:
    - dannixon.dotfiles.git
    - dannixon.dotfiles.neovim
    - dannixon.dotfiles.tmux
    - dannixon.dotfiles.zsh
